# Core agent framework
crewai==1.14.5  # 1.14.6+ needs lancedb>=0.29 (no build for this platform)
crewai-tools>=0.8.0

# LLM providers (CrewAI 1.x uses LiteLLM internally)
openai>=1.0.0
anthropic>=0.7.0

# Data collection — free APIs only
yfinance>=0.2.0
pandas>=2.0.0
numpy>=1.24.0
requests>=2.31.0
beautifulsoup4>=4.12.0
lxml>=4.9.0
feedparser>=6.0.0

# Configuration and validation
pydantic>=2.0.0
pydantic-settings>=2.0.0
python-dotenv>=1.0.0

# Statistics (trend regression in analysis tools)
scipy>=1.11.0

# Report generation
jinja2>=3.1.0
markdown>=3.5.0
pymdown-extensions>=10.0

# Search-interest sentiment (Google Trends, best-effort)
pytrends>=4.9.0

# Caching (graceful fallback if Redis unavailable)
redis>=5.0.0

# Local web UI (FastAPI backend + no-build frontend; `python -m stock_analysis.web`)
fastapi>=0.110.0
uvicorn[standard]>=0.27.0

# Development and testing
pytest>=7.4.0
pytest-asyncio>=0.21.0
pytest-cov>=4.1.0
black>=23.0.0
isort>=5.12.0
flake8>=6.0.0
mypy>=1.5.0
